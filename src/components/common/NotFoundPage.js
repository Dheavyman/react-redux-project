import React from 'react';
import { Link } from 'react-router';

/**
 * Not found page component
 *
 * @returns {object} React element
 */
const NotFoundPage = () => (
  <div>
    <h3>Whoops!, You have reached the end of the internet</h3>
    <p>Page not found.</p>
  </div>
);

export default NotFoundPage;
